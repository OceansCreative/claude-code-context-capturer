---
name: Bug report
about: Something captured wrong, didn't capture, or behaved unexpectedly
title: '[bug] '
labels: bug
---

## What you expected to happen

<!-- e.g. "Capturing a GitHub Issue should land its body in CLAUDE.md" -->

## What actually happened

<!-- e.g. "Nothing appeared in the file" or "The captured Markdown was missing the comments section" -->

## Reproduction

1.
2.
3.

## Environment

- Extension version: <!-- chrome://extensions/ → Details → Version (e.g. v0.5.0) -->
- Chrome version: <!-- chrome://version/ → Google Chrome -->
- Output mode: <!-- Copy to clipboard / Append to buffer / Append to linked CLAUDE.md file / Both -->
- OS: <!-- macOS 15.x / Windows 11 / Ubuntu 24.04 -->

## Service worker console output

<!-- 1. Open chrome://extensions/
     2. Find "Claude Code Context Capturer"
     3. Click "Service worker" (or "service worker (Inactive)" → it'll wake up)
     4. Paste any [CCC] error lines or stack traces below.
     The extension always prefixes its console output with [CCC]. -->

```
```

## Page being captured

<!-- URL, if shareable. If the bug is specific to a private page, a screenshot
     of the rendered Markdown (or pasted output) is the most useful substitute. -->

## Anything else

<!-- e.g. "Only happens when popup is open", "Started after v0.4.x", "Worked once, then broke" -->
