node_modules/
dist/
.DS_Store
*.log
.env
.env.local
.vite
coverage/
*.zip
# .claude/ — selectively ignored so shared config (settings.json,
# commands/, agents/) can be committed while local/runtime files stay out.
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Ephemeral puppeteer verification scripts
cccc-verify-*.mjs
